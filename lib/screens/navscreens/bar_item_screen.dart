import 'package:flutter/material.dart';

class BarItemScreen extends StatelessWidget {
  const BarItemScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("BarItem Screen"),);
  }
}
